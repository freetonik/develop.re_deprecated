<ol class="stories list">
  <%= render :partial => "stories/listdetail", :collection => @stories,
    :as => :story %>
</ol>

<div class="morelink">
  <% if @page && @page > 1 %>
    <a href="<%= @tag ? "/t/#{@tag.tag}" : (@newest ? "/newest" +
      (@for_user ? "/#{@for_user}" : "") : "") %><%= @page == 2 ? "/" :
      "/page/#{@page - 1}" %>">&larr; Страница <%= @page - 1 %></a>
  <% end %>

  <% if @show_more %>
    <% if @page && @page > 1 %>
      |
    <% end %>
    <a href="<%= @tag ? "/t/#{@tag.tag}" : (@newest ? "/newest" +
      (@for_user ? "/#{@for_user}" : "") : "") %>/page/<%= @page + 1 %>">Страница
      <%= @page + 1 %> &rarr;</a>
  <% end %>
</div>

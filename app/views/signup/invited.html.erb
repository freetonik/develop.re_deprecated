<div class="box wide">
  <div class="legend">
    Регистрация
  </div>

  <%= form_for @new_user, { :url => signup_url,
  :autocomplete => "off" } do |f| %>
    <%= hidden_field_tag "invitation_code", @invitation.code %>

    <p>
    Введите свой e-mail и пароль. Ваш e-mail никогда не будет показан другим пользователям
    и будет использован только для восстановления пароля и, если захотите, для оповещений.
    Имя можно будет поменять позже.
    </p>

    <%= error_messages_for(@new_user) %>

    <p>
    <%= f.label :invitation, "Приглашение от:", :class => "required" %>
    <span class="d">
      <a href="/u/<%= @invitation.user.username %>" target="_blank"><%=
        @invitation.user.username %></a>
    </span>
    </p>

    <p>
    <%= f.label :username, "Имя:", :class => "required" %>
    <%= f.text_field :username, :size => 30 %>
    <span class="hint">
      <tt>[A-Za-z0-9][A-Za-z0-9_-]*</tt>
    </span>
    <br />

    <%= f.label :email, "E-mail:", :class => "required" %>
    <%= f.email_field :email, :size => 30 %>
    <span class="hint">
      Аватар берется из <a href="http://www.gravatar.com/" target="_blank">Gravatar</a>
    </span>
    <br />

    <%= f.label :password, "Пароль:", :class => "required" %>
    <%= f.password_field :password, :size => 30, :autocomplete => "off" %>
    <br />

    <%= f.label :password_confirmation, "Пароль (еще разок):",
      :class => "required" %>
    <%= f.password_field :password_confirmation, :size => 30,
      :autocomplete => "off" %>
    <br />
    </p>

    <p>
    Расскажите о себе, если хотите. Эта информация будет видна всем в вашем публичном профиле.
    Ее можно изменить в любой момент.
    </p>

    <p>
    <div class="boxline">
      <%= f.label :about, "Обо мне:", :class => "required" %>
      <%= f.text_area :about, :size => "100x5", :style => "width: 600px;" %>
    </div>

    <div class="box">
      <div class="boxline markdown_help_toggler"
        style="margin-left: 9em; width: 600px;">
        <div class="markdown_help_label">
          Можно использовать Markdown.
        </div>

        <div style="clear: both;"></div>

        <%= render :partial => "global/markdownhelp" %>
      </div>
    </div>

    <p>
    <%= submit_tag "Зарегистрироваться" %>
    </p>
  <% end %>
</div>
